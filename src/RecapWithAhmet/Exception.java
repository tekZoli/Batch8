package RecapWithAhmet;

public class Exception {

    //1.what is the diffrenece between final, finalize, finally
    //final: is the KEYWORD to make your method and variables, classes unchangeable
    //finalize: is a METHOD to to make sure gc is working or not
    //finally is a BLOCK that comes after try and catch
    //*it show the final execution format

    //2.how do you handle exceptions
    //Throws--> after the method signature and TryCatch inside of the block
    //Throw inside the method

    //3what is the diff between error and exception
    //ERROR: you cannot handle it, comes from he system
    //EXEPTION: you can handle it because it comes from the
}
