package RecapWithAhmet;

public class StaticKeyWord {

    //what is static keyword
    //is keyword to make the methods belong to the class
    //the advantage of having static keyword that you do not need to create an object to access the variables
    //or methods
    //the disadvantage is that you cannot override the methods  once you call it

    //what is static block
    // is the block that can be called once and will be executed first

    //what is static variable:
    //it makes the variable belong to class
    //**it is shareable/changeable --> common for each object
    //it is access by other classes without creating an object of it

}
